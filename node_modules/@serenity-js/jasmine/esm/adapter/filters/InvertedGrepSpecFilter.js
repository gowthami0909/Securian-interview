/**
 * @package
 */
export class InvertedGrepSpecFilter {
    pattern;
    constructor(pattern) {
        this.pattern = new RegExp(pattern);
    }
    matches(specName) {
        return !this.pattern.test(specName);
    }
}
//# sourceMappingURL=InvertedGrepSpecFilter.js.map